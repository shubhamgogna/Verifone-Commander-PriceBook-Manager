<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="VerifoneCommander.PriceBookManager.DesktopApp.AccountPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VerifoneCommander.PriceBookManager.DesktopApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Style TargetType="local:ValidatedTextBox">
            <Setter Property="HorizontalAlignment" Value="Left" />
        </Style>
    </Page.Resources>

    <StackPanel>
        <local:ValidatedTextBox ViewModel="{x:Bind ViewModel.Hostname}"
                                Header="Hostname"
                                Placeholder="Enter ConfigClient hostname (ex: 192.168.31.11)"/>
        <local:ValidatedTextBox ViewModel="{x:Bind ViewModel.Username}"
                                Header="Username"
                                Placeholder="Enter your username"/>
    </StackPanel>
</Page>
